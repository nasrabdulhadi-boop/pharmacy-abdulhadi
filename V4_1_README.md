# صيدلية عبدالهادي — v4.1 Complete

هذه النسخة تجمع مراحل v3.7 → v4.1 في مشروع واحد.

## ما تم بناؤه
- Dashboard احترافي: مبيعات اليوم، الطلبات، الوصفات، الأصناف، منخفض المخزون، المنتهي، وقريب الانتهاء.
- رسم بياني للمبيعات آخر 7 أيام.
- مركز تنبيهات ذكي مرتبط بالمخزون والطلبات والوصفات.
- نظام وصفات: رفع صورة/PDF، اسم، هاتف، ملاحظات، حالات (جديدة → مراجعة → تجهيز → تسليم)، وروابط مؤقتة للعرض.
- نظام طلبات حقيقي مع Order ID مثل ORD-1025، سلة، بيانات العميل، حالة الطلب، وتتبع برقم الطلب + الهاتف.
- واتساب ذكي بخيارات متعددة.
- طلب "لم تجد الدواء؟" يصل إلى لوحة الإدارة.
- مفضلة محلية وإعادة آخر طلب.
- عروض تظهر تلقائياً في الموقع وتدار من لوحة الإدارة.
- تقارير مبيعات/مشتريات/ربح تقديري/قيمة مخزون/الأكثر والأقل مبيعاً.
- تصدير Excel بصيغة CSV المتوافقة مع Excel + طباعة PDF من المتصفح.
- باركود: بحث بالرقم + ماسح كاميرا في نقطة البيع عند دعم BarcodeDetector.
- نقطة بيع مرتبطة بـ complete_sale_atomic وFEFO.

## مهم جداً قبل النشر
1. ارفع ملفات المشروع إلى GitHub كما هي.
2. في Supabase SQL Editor شغّل الملف:
   `V4_1_UPGRADE.sql`
3. تأكد أن مستخدم الإدارة لديه:
   `raw_app_meta_data.role = admin`
4. في Vercel اترك:
   - `VITE_SUPABASE_URL`
   - `VITE_SUPABASE_PUBLISHABLE_KEY`
5. إذا كان مجلد المشروع داخل GitHub هو `pharm_final`، اجعل Root Directory في Vercel = `pharm_final`.
6. بعد نجاح Deploy افتح الموقع واختبر:
   - تسجيل الإدارة
   - Dashboard
   - إضافة منتج + دفعة
   - نقطة البيع + باركود
   - طلب من الموقع
   - تتبع الطلب
   - وصفة من الموقع
   - العروض
   - التقارير

## ملاحظة الأسعار
واجهة الزبون في v4.1 تعرض "السعر عند الطلب" ولا تعرض سعر البيع مباشرة، مع بقاء سعر البيع محفوظاً في قاعدة البيانات لاستخدام الطلبات ونقطة البيع والتقارير.

## ملاحظة ملفات الوصفات
Bucket `prescriptions` يبقى Private. العميل يستطيع الرفع فقط إلى مسار `intake/`، بينما القراءة والتنزيل محصوران بالإدارة عبر Signed URLs.
